{
  "name": "school-saas",
  "private": true,
  "version": "0.0.0",
  "description": "Multi-tenant school management SaaS for the Indian K-12 market",
  "type": "module",
  "engines": {
    "node": ">=20.19"
  },
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "dev": "npm-run-all --parallel dev:api dev:web",
    "dev:api": "npm run dev --workspace @school/api",
    "dev:web": "npm run dev --workspace @school/web",
    "build": "npm run build --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "lint": "npm run lint --workspaces --if-present",
    "db:generate": "npm run generate --workspace @school/db",
    "db:migrate": "npm run migrate --workspace @school/db",
    "db:seed": "npm run seed --workspace @school/db",
    "infra:up": "docker compose up -d",
    "infra:down": "docker compose down"
  },
  "devDependencies": {
    "npm-run-all": "4.1.5",
    "typescript": "5.7.3"
  }
}
